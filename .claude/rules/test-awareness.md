<!-- Always-on rule: no `paths:` frontmatter → loaded unconditionally every session
     (mirrors Cursor's `alwaysApply: true`). Single source of truth:
     .cursor/rules/test-awareness.mdc — edit the rule body there. -->

@../../.cursor/rules/test-awareness.mdc
