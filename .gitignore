# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
package

# Env
.env
.env.local
.env.development
.env.production
.env.test

# Editors
dist
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Test
coverage/
.nyc_output/

# Firebase emulator logs
firestore-debug.log
firebase-debug.log

# Starlight / Astro docs site (website/ has its own package + lockfile)
website/dist/
website/.astro/
website/node_modules/

./src/schemas
./core/TransactionContext.ts
.src/firebase-service-account.json
test.ts

# ai reviews and plans, etc
tmp/

# npm pack tarballs (created by release verification scripts)
*.tgz

# Temporary backup of the GitHub README while prepack stages npm-readme.md for the tarball
.README.github.bak

# Agent tooling — ignore local/transient state under the generated trees; keep
# rulesync outputs (and optional shared Cursor hooks) trackable. New junk from
# Cursor / Claude Code / Codex is ignored by default. Un-ignore both the
# directory and its contents (`!dir/` + `!dir/**`) so nested files are not
# blocked by the parent `.cursor/*` / `.claude/*` / `.agents/*` exclude.
#
# Keep trackable (rulesync):
#   .cursor/{rules,commands,skills}/
#   .claude/{rules,commands,skills}/
#   .agents/{memories,skills}/
# Stay local (examples): .cursor/debug.log, .cursor/plans/, .cursor/mcp.json,
#   .claude/settings.local.json, .claude/worktrees/, .claude/agent-memory-local/
.cursor/*
!.cursor/rules/
!.cursor/rules/**
!.cursor/commands/
!.cursor/commands/**
!.cursor/skills/
!.cursor/skills/**
!.cursor/hooks.json
!.cursor/hooks/
!.cursor/hooks/**

.claude/*
!.claude/rules/
!.claude/rules/**
!.claude/commands/
!.claude/commands/**
!.claude/skills/
!.claude/skills/**

.agents/*
!.agents/memories/
!.agents/memories/**
!.agents/skills/
!.agents/skills/**

# Root-level personal overrides (Claude Code + Codex) — never commit
CLAUDE.local.md
**/AGENTS.override.md
